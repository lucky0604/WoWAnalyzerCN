# Ignore everything; whitelist what the multi-stage build actually needs.
# Build context is the repo root (see docker-compose.yml).
*

!mcp-server/
mcp-server/**/node_modules
mcp-server/**/dist
mcp-server/.copied
mcp-server/.env
mcp-server/.env.*
mcp-server/Dockerfile
mcp-server/docker-compose.yml

!src/
src/**/node_modules
src/**/dist

!tsconfig.json
!package.json
!pnpm-lock.yaml
