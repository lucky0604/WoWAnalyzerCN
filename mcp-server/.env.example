# WCL API base URL — points to the local wcl-proxy-server
# Start wcl-proxy-server separately before using this MCP server
WCL_API_BASE=http://localhost:9528/v1

# Optional: set to production mode
# NODE_ENV=production
